source "https://rubygems.org"

group :development do
  gem 'aws-sdk-s3'
  gem 'dotenv' # Para gerenciar suas chaves sem expô-las no GitHub
  gem 'rake'
end

gem "github-pages", group: :jekyll_plugins
